---
title: |
  Cocktails
description: |
  Pairwise correlation, Network diagram, Principal component analysis (PCA)
date: 2020-05-25
date-format: long
---

Notable topics: Pairwise correlation, Network diagram, Principal component analysis (PCA)

Recorded on: 2020-05-25

Timestamps by: Eric Fletcher

[View code](https://github.com/dgrtwo/data-screencasts/blob/master/cocktails.Rmd)

## Full screencast

<iframe class="video" src="https://www.youtube.com/embed/EC0SVkFB2OU" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>

## Timestamps

<script id="timestamps" type="application-json">[{"timestamp_with_hours":"0:6:20","description":"Use `fct_reorder` from the `forcats` package to reorder the `ingredient` factor levels along `n`.","functions":"fct_reorder","packages":"forcats","timestamp_sec":380},{"timestamp_with_hours":"0:7:40","description":"Use `fct_lump` from the `forcats` package to lump together all the levels except the `n` most frequent in the `category` and `ingredient` variables.","functions":"fct_lump","packages":"forcats","timestamp_sec":460},{"timestamp_with_hours":"0:11:30","description":"Use `pairwise_cor` from the `widyr` package to find the correlation between the `ingredients`.","functions":"pairwise_cor","packages":"widyr","timestamp_sec":690},{"timestamp_with_hours":"0:16:00","description":"Use `reorder_within` from the `tidytext` package with `scale_x_reordered`  to reorder the the columns in each `facet`.","functions":"reorder_within | scale_x_reordered","packages":"tidytext","timestamp_sec":960},{"timestamp_with_hours":"0:19:45","description":"Use the `ggraph` and `igraph` packages to create a `network diagram`","functions":"graph_from_data_frame | ggraph | geom_edge_link | geom_node_point | geom_node_label","packages":"igraph | ggraph","timestamp_sec":1185},{"timestamp_with_hours":"0:25:15","description":"Use `extract` from the `tidyr` package with `regex = (.*) oz` to create a new variable `amount` which doesn't include the `oz`.","functions":"extract","packages":"tidyr","timestamp_sec":1515},{"timestamp_with_hours":"0:26:40","description":"Use `extract` with `regex` to turn the strings in the new `amount` variable into separate columns for the `ones`, `numerator`, and `denominator`.","functions":"extract","packages":"tidyr","timestamp_sec":1600},{"timestamp_with_hours":"0:28:53","description":"Use `replace_na` from the `tidyr` package to replace `NA` with zeros in the `ones`, `numberator`, and `denominator` columns. David ends up reaplcing the `zero` in the `denominator` column with ones in order for the calculation to work.","functions":"replace_na","packages":"tidyr","timestamp_sec":1733},{"timestamp_with_hours":"0:31:49","description":"Use `geom_text_repel` from the `ggrepel` package to add `ingredient` labels to the `geom_point` plot.","functions":"geom_text_repel","packages":"ggrepel","timestamp_sec":1909},{"timestamp_with_hours":"0:32:30","description":"Use `na_if` from the `dplyr` package to replace `zeros` with `NA`","functions":"na_if","packages":"dplyr","timestamp_sec":1950},{"timestamp_with_hours":"0:34:25","description":"Use `scale_size_continuous` with `labels = percent_format()` to convert size legend values to percent.","functions":"scale_size_continuous","packages":"ggplot2","timestamp_sec":2065},{"timestamp_with_hours":"0:36:35","description":"Change the size of the points in the `network diagram` proportional to `n` using `vertices = ingredient_info` within `graph_from_data_frame` and `aes(size = n)` within `geom_node_point`.","functions":"graph_from_data_frame | geom_node_point","packages":"igraph | ggraph","timestamp_sec":2195},{"timestamp_with_hours":"0:48:05","description":"Use `widely_svd` from the `widyr` package to perform principle component analysis on the `ingredients`.","functions":"widely_svd | top_n | abs | geom_col | reorder_within | scale_y_reordered | facet_wrap","packages":"widyr","timestamp_sec":2885},{"timestamp_with_hours":"0:52:32","description":"Use `paste0`  to concatenate `PC` and `dimension` in the facet panel titles.","functions":"paste0","packages":"base","timestamp_sec":3152},{"timestamp_with_hours":"0:57:00","description":"Summary of screencast.","timestamp_sec":3420}]</script>
